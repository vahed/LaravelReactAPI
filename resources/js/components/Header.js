import React from 'react';
import { BrowserRouter, Route, Link } from 'react-router-dom';

const Header = () => (
    <nav className='navbar navbar-dark bg-dark'>
            <span className="navbar-brand mb-0 h1">My Task</span>
    </nav>
)

export default Header;
